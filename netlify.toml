# Netlify build configuration for Hugo
[build]
  publish = "public"
  command = "hugo --gc --minify"

[build.environment]
  HUGO_VERSION = "0.145.0"

[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
